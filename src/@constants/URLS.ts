export enum URL {
  SERVICES = '/services',
  SERVICE_WORKERS = '/service-workers',
}

export enum AUTH {
  LOGIN = "/auth/login",
  REGISTER = "/auth/register"
}

export enum USER {
  UPDATE_PHOTO = "/user/update-photo",
  UPDATE = "/user/update",
  ME = "/user/me",
}